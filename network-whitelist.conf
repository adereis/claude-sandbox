# Claude Sandbox - Network Whitelist (default)
# ERE regex patterns matched against request domains.
# Use (^|\.)domain\.com$ to match domain and all subdomains.

# === Claude Code Core ===
(^|\.)api\.anthropic\.com$
(^|\.)claude\.ai$
(^|\.)console\.anthropic\.com$
(^|\.)platform\.claude\.com$

# === Telemetry & Feature Flags ===
# Removing these may affect functionality (feature flags control behavior).
# Set CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1 to disable instead.
(^|\.)statsig\.anthropic\.com$
(^|\.)statsigapi\.net$
(^|\.)sentry\.io$

# === Package Management ===
(^|\.)registry\.npmjs\.org$

# === GitHub Integration ===
(^|\.)github\.com$
(^|\.)api\.github\.com$
(^|\.)raw\.githubusercontent\.com$
